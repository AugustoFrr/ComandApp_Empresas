import * as firebaseApp from "firebase/app";
import "firebase/database";
import "firebase/auth";
var firebaseConfig = {
    //INFORMAÇÕES INDISPONÍVEIS POR SEGURANÇA

};

firebaseApp.initializeApp(firebaseConfig);
export default firebaseApp;
